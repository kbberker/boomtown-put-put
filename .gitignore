# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env / secrets (HOLE_PIN lives in Netlify env, never committed)
.env
.env.*
!.env.example

# Design reference bundle — consult locally, never commit (copy what's
# needed out of it instead, e.g. the optimized logo in src/assets/).
src/assets/design_handoff_boomtown_ui/

# Notes from human
docs/future-work.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Deno lockfile from `netlify dev`'s edge runtime (no edge functions here)
deno.lock
