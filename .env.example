# Copy to `.env` (gitignored) for local `pnpm dev:netlify`.
# The editor PIN gating PUT /api/holes. For local dev this can be any value —
# it only guards the local Blobs sandbox, not production (ADR-0003).
HOLE_PIN=
